entity User;
entity Document;

action "view" appliesTo {
    principal: User,
    resource: Document,
    context: {},
};

action "edit" in [Action::"view"] appliesTo {
    principal: User,
    resource: Document,
    context: {},
};
