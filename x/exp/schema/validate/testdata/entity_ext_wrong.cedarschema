entity User {
    ip_addr: ipaddr,
    amount: decimal,
    created: datetime,
    ttl: duration,
};
entity Document;

action "view" appliesTo {
    principal: User,
    resource: Document,
    context: {},
};
