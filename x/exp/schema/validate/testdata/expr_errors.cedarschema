entity User {
    name: String,
    age: Long,
    active: Bool,
};

entity Document {
    title: String,
};

action "view" appliesTo {
    principal: User,
    resource: Document,
    context: {},
};
